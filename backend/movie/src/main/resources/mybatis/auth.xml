<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.movie.repository.UserMapper">
    <select id="getUser">
        SELECT * FROM MEMBERS WHERE user_id = #{userId};
    </select>

    <select id="existsId">
        SELECT COUNT(*) FROM MEMBERS WHERE USER_ID = #{userId}
    </select>
    <select id="existsEmail">
        SELECT COUNT(*) FROM MEMBERS WHERE EMAIL = #{email}
    </select>

    <insert id="signup">
        INSERT INTO MEMBERS (USER_ID, PASSWD, USER_NAME, EMAIL, TEL, USER_TPCD)
        VALUES (#{userId}, #{passwd}, #{userName}, #{email}, #{tel}, "1")
    </insert>
</mapper>